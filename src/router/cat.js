


    import cat from "@/components/cat"

    const catt = [
        //注册页面的路由
        {
            path: "/cat",
            name: "cat",
            component: cat,
        }
    ];
    
    //抛出shop对象
    export default catt;